# Integrating CodeCompanion with athenahealth

*A HIPAA-aware setup guide — run it for **your own practice**, alongside athena.*
*Prepared for Matt · 2026-06-20 · Internal / Confidential · Engineering guidance, not legal advice.*

> **Scope: a single practice, your own.** This guide assumes you are running CodeCompanion for your
> own clinic only — not selling it to other practices. That removes the heavy "vendor" obligations
> (Marketplace certification, HITRUST, the cross-tenant data questions); those live in **Appendix A —
> If you sell it later**, so nothing is lost if the plan changes.
>
> The branded PDF is generated by `npm run athena-pdf` (`scripts/athena-integration-pdf.ts`).
> Edit this markdown as the source of truth, then regenerate.

The goal is a fast, safe win: the office-manager cockpit and clinical intelligence live on athena's
data this quarter, with billing left to athena — on a **low-cost, HIPAA-capable stack** (~$50–100/mo,
not the ~$950/mo of the Vercel + Supabase HIPAA tiers).

---

## 1. The integration model — and why billing stays off

athenahealth stays your **system of record and your biller**. CodeCompanion sits beside it as the
office-manager **intelligence layer**, with its own billing rails switched **off**. CodeCompanion
pulls clinical encounters and charges from athena into its canonical model and runs its cockpit,
clinical intelligence, and eligibility on top — it never submits a claim for an athena practice and
never writes back to athena billing.

> **Why billing is off — this is by design, not a limitation.**
> athenahealth locks claim submission to its own clearinghouse (athenaEDI / ECR). Third-party
> billing through any other clearinghouse is **not permitted** — it is how athena monetizes the
> platform. So CodeCompanion runs in **"Clinical only (EHR billing)"** mode: one click in
> **Admin → Module control** turns the whole Billing & RCM group off.

**What you gain right now**

- **The cockpit.** Command Center, Follow-up Queue, and Dashboard — the office-manager command center over athena's day.
- **Clinical intelligence on athena encounters.** Coding suggestions, Care Gaps, Audit Shield, Practice Pulse, Schedule Risk, and Revenue Analytics.
- **Real-time eligibility.** 270/271 verification via Stedi — read-only, no clearinghouse enrollment, and it does not conflict with athena's billing.
- **Found-money analytics (Phase 2).** Diff athena's submitted-vs-paid to surface underpayments, denials, and undercoding — read-only; it never re-submits a claim.

**Data flow (one-way pull)**

```
athena API                          CodeCompanion
  /claims, /patients,    ───▶  canonical model  ───▶  modules
  /insurances, /providers      (EhrEncounter → Claim)    (cockpit, intelligence, eligibility)
  (encounters + charges)

  # PHI is pulled IN and normalized. Nothing is written back to athena.
```

---

## 2. HIPAA, PHI & BAAs (read before any real data)

You are still handling your patients' PHI, so HIPAA fully applies even for a single practice.
CodeCompanion treats compliance as **architecture, not a checklist** — the platform must not process
real patient data until the gate below is closed. Until then everything runs on synthetic data
(`ATHENA_USE_MOCK=true`, `ALLOW_REAL_PHI=false`), the mock-first posture every adapter follows.

**The hard gate (from `COMPLIANCE.md`)**

- **BAAs signed** with every vendor in the PHI path (the table below).
- **Audit logging live** on every PHI access (already built: an immutable, append-only audit log).
- **Encryption + access control** on the data plane (Postgres RLS is already built and CI-guarded).
- **`ALLOW_REAL_PHI`** stays `false` until the above is true; it gates every real-PHI rail, including
  the live athena client.

**Business Associate Agreements you need** — the low-cost stack (Cloud Run + Neon + Supabase Auth):

| Vendor | Role in the PHI path | BAA needed? | Cost note |
|---|---|---|---|
| **athenahealth** | Source of clinical encounters + charges (PHI in) | Yes — usually via your athenaOne agreement | — |
| **Google Cloud (Cloud Run)** | Hosts/serves the app (renders PHI) | Yes — **free**, via Google Cloud | ~$0–40/mo (scales to zero) |
| **Neon** | Postgres database storing PHI at rest | Yes — **included** on the Scale tier | ~$20–70/mo |
| **Anthropic** | Language tasks only (appeal / narrative drafting) | Yes | usage-based |
| **Stedi** | Clearinghouse for 270/271 eligibility (PHI to payers) | Yes | usage-based |
| **Supabase** (Auth only) | Staff logins — **no patient PHI** | **No** — no PHI flows here | ~$25/mo Pro |
| **Stripe** | Card payments (only if tied to PHI-linked balances) | Yes, if applicable | usage-based |

> **One structural question for counsel.** Is CodeCompanion *internal* to the practice (same legal
> entity → no extra BAA between it and the practice) or a *separate* tool/company serving the practice
> (→ a BAA between CodeCompanion and the practice)? Either way the cloud vendors above still need BAAs.
> Five-minute check, but worth doing.

**athena-specific compliance notes**

- **Minimum necessary.** The adapter requests only the MDP ("More Disruption Please") service scope
  (`athena/service/Athenanet.MDP.*`) and pulls only the encounter, charge, insurance, and provider
  fields needed to build a claim — nothing more.
- **The adapter boundary contains PHI.** athena field names never leave `lib/adapters/athena`;
  everything normalizes into the canonical model, tenant-isolated under RLS.
- **Key security (athena ToS §2).** Keep `client_id`/`client_secret` server-side only (env vars,
  never shipped to the client); restrict access to employees who need them; never share or transfer
  keys; and **notify `MDPDev@athenahealth.com` on any compromise**.
- **Read the agreements before signing.** Review athena's data terms, the Master Services Agreement
  Third-Party Terms, and your athenaOne agreement alongside the BAA. Note athena exposes structured
  charge/payment objects, **not** raw X12 837/835 files.

> **Not legal advice.** This is engineering guidance to keep the build compliant-by-construction. It
> is not a substitute for a HIPAA security risk assessment or counsel. Get a qualified review and
> proper BAA sign-off before any real PHI flows.

---

## 3. Prerequisites (athena-specific)

Supabase Auth, Google Cloud, Neon, and GitHub are assumed set up (see section 5 / the code repo for
the stack). What is specific to athena:

- **Developer account.** Register at developer.athenahealth.com.
- **A registered application.** Create an app to obtain a `client_id` and `client_secret`. Start in Preview (sandbox).
- **Your practice + department.** The athenaNet `practiceid` (the Preview sandbox practice is `195900`) and at least one `departmentid`.
- **The MDP scope.** Request the `athena/service/Athenanet.MDP.*` scope on the app.

> **Your access path (own practice).** Preview/sandbox is instant and free (practice `195900`, dummy
> data). For your real data, athena issues **production credentials** for your practice — a light step
> (no Marketplace listing or certification, since you are not distributing to other practices). See
> Appendix A if that ever changes.

You will set the athena environment variables (section 5) plus the stack's `DATABASE_URL` (Neon).

---

## 4. API integration steps

### Step 1 — Register the app in Preview
At developer.athenahealth.com, open the **Developer Console → Create New Application**, then record
the `client_id` and `client_secret`. **Preview data is synthetic**, so connecting is safe before any
BAA. The form has three choices that decide whether the app can even reach billing data — set them
like this:

- **API Access → "1+ non-Certified APIs."** The billing/charge/claim data lives in athena's
  **non-Certified** proprietary REST (athenaCollector); the Certified (g)(8)–(g)(10) FHIR APIs are
  clinical-only and will **not** carry it. "Certified APIs only" locks you out of the data you need.
- **App Category → "2-Legged OAuth"** (service-to-service / `client_credentials`) — exactly what the
  adapter uses. (3-legged is for SMART-on-FHIR provider/patient login, which we do not use.)
- **Authentication → "Secret"** (a `client_secret`) — matches the adapter, which authenticates with
  `Authorization: Basic base64(client_id:client_secret)`. (JWK / `private_key_jwt` is more secure for
  production and an easy future swap — see section 6 — but Secret is the right call to get connected.)
- **Do not check "I am building a CAPI app"** — that is for ONC-Certified API apps, not a
  non-certified service app. On **App Details**, request the `athena/service/Athenanet.MDP.*` scope.

### Step 2 — Identify practice + department
Use Preview `practiceid` `195900` to start. For production, use your real `practiceid` and
`departmentid`(s).

### Step 3 — Configure the environment
Set these in your staging deployment first (full table in section 5):

```bash
ATHENA_CLIENT_ID=...                       # from the Preview app
ATHENA_CLIENT_SECRET=...
ATHENA_PRACTICE_ID=195900                  # Preview sandbox practice
ATHENA_BASE_URL=https://api.preview.platform.athenahealth.com
ATHENA_USE_MOCK=false                      # use the real client (against Preview)
```

> **Sandbox vs. PHI — flip `ALLOW_REAL_PHI` in staging only.** The live athena client is gated
> behind `ALLOW_REAL_PHI`. athena Preview is synthetic, so to exercise the live client against
> Preview you set `ALLOW_REAL_PHI=true` in a **non-production staging deploy only** — and keep every
> other real rail mock (`CLEARINGHOUSE=mock` or `STEDI_SANDBOX=true`, no live Stripe key). **Never**
> set `ALLOW_REAL_PHI=true` in production until the BAA gate in section 2 is fully closed.

### Step 4 — Validate OAuth + the encounter pull
The adapter performs an OAuth2 client-credentials exchange (token cached until ~60s before expiry),
then pulls a day of claims and joins each to its patient, insurance, and provider:

```ts
import { createAthenaSource, athenaConfigFromEnv, pullAthenaEncounters } from '@/lib/adapters/athena'

const source = createAthenaSource(athenaConfigFromEnv())
const encounters = await pullAthenaEncounters(source,
  { serviceDateFrom: '2026-06-01', serviceDateTo: '2026-06-01' })
```

### Step 5 — Verify the canonical mapping
`pullAthenaEncounters` yields canonical `EhrEncounter`s; `encounterToClaim(e, 'athena')` turns each
into a canonical `Claim`. Confirm CPT/HCPCS, ICD-10 pointers, charge cents, subscriber, and payer
populate correctly.

> **One crosswalk to wire: athena payer id → EDI payer id.** athena's `insurancepackageid` is
> athena-internal. For real-time eligibility through Stedi, resolve it to the clearinghouse/EDI payer
> id via the `PayerDirectory` at the submission edge. (See section 6.)

### Step 6 — Turn billing off, then choose your modules
In **Admin → Module control**, click **"Clinical only (EHR billing)"** to turn the entire Billing &
RCM group off. Then re-enable the read-only analytics you want running alongside athena:

| Modules | State | Why |
|---|---|---|
| Command Center, Follow-up Queue, Dashboard | **On** | The cockpit — your daily drivers over athena's day. |
| Coding, Care Gaps, Audit Shield, Practice Pulse, Schedule, Analytics | **On** | Clinical intelligence on athena encounters. Read-only. |
| EHR Pull | **On** | Pulls encounters from athena into the canonical model. |
| Eligibility | **On** | Real-time 270/271 via Stedi. Read-only; no enrollment. |
| Found Money, A/R & Denials | **On (Phase 2)** | Analytics on athena's submitted-vs-paid. Turn on once payments are wired (section 6). |
| Claims (RCM), Clearinghouse, Enrollments, Scrubber, Patient Billing, Patient Balances | **Off** | athena owns billing and the clearinghouse. Do not run these. |

### Step 7 — Go to production
When the BAA gate is closed: sign the BAAs (section 2), deploy the app to **Cloud Run** and point it
at your **Neon** database, set the real athena values, and flip the gate:

```bash
ATHENA_BASE_URL=https://api.platform.athenahealth.com   # production
ATHENA_PRACTICE_ID=<your real practiceid>
ATHENA_USE_MOCK=false
ALLOW_REAL_PHI=true            # only after BAAs + security review
DATABASE_URL=postgres://...neon...   # HIPAA Neon (Scale tier)

# then:  npm run readiness     # env-aware go/no-go check
```

> **Production access is not just a URL swap.** athena issues **separate production credentials**
> (distinct from your Preview keys) through its partner / MDP process. Request them early — that
> approval is the long pole on the timeline.

---

## 5. Reference — OAuth, endpoints, environment variables

**What the adapter calls**

```
POST {base}/oauth2/v1/token
   Authorization: Basic base64(client_id:client_secret)
   grant_type=client_credentials&scope=athena/service/Athenanet.MDP.*

GET  {base}/v1/{practiceid}/claims?servicedatefrom=MM/DD/YYYY&servicedateto=..
GET  {base}/v1/{practiceid}/patients/{patientid}
GET  {base}/v1/{practiceid}/patients/{patientid}/insurances
GET  {base}/v1/{practiceid}/providers/{providerid}

# base = Preview  https://api.preview.platform.athenahealth.com
#        Prod     https://api.platform.athenahealth.com
```

**Environment variables**

| Variable | Staging / Preview | Notes |
|---|---|---|
| `ATHENA_USE_MOCK` | `false` | `true` = synthetic mock (default). `false` = real client. |
| `ALLOW_REAL_PHI` | `true` (staging only) | Master gate. Production: only after BAAs + review. |
| `ATHENA_CLIENT_ID` | `<preview app id>` | OAuth client id from developer.athenahealth.com. |
| `ATHENA_CLIENT_SECRET` | `<preview secret>` | OAuth client secret. Server-side only. |
| `ATHENA_PRACTICE_ID` | `195900` | Preview practice; your real `practiceid` in prod. |
| `ATHENA_BASE_URL` | preview URL | Preview vs. production base (see above). |
| `DATABASE_URL` | `<neon staging>` | Postgres (Neon). PHI at rest — HIPAA Scale tier in prod. |

---

## 6. What's built vs. what's next (honest scope)

**Built in this repo** (`lib/adapters/athena`)

- **OAuth2 client.** Client-credentials token exchange with caching; injectable transport (unit-tested without a network).
- **Encounter + charge pull.** `getEncounterBundles` joins claims to patient, insurance, and provider for a service-date range.
- **athena → canonical mapping.** `athenaBundleToEhrEncounter` → `EhrEncounter` → `encounterToClaim`. Money as integer cents; no athena type escapes the adapter.
- **Mock source + tests.** A synthetic `MockAthenaSource` and six passing tests covering OAuth, the join, and the mapping.
- **The billing-off control.** Admin module control + the "Clinical only (EHR billing)" preset, built for exactly this integration.

**Next, to complete the athena value loop**

1. **Wire EHR Pull to a sync.** Connect `pullAthenaEncounters` to the EHR Pull screen and/or a scheduled job (Inngest) so encounters land daily.
2. **Payer-id crosswalk.** Resolve athena `insurancepackageid` → EDI payer id at the eligibility edge so one-click eligibility works on athena patients.
3. **Phase 2 — pull payments.** Pull athena payment/remittance objects and map them to the canonical remittance to power the Found-Money diff (submitted vs. paid vs. contracted) on athena's own data. This is the high-ROI "found money" proof, and it is read-only.
4. **Add 429 backoff to the sync.** athena's production rate limits are not published numerically (HTTP 429 on throttle), so the daily pull should retry with exponential backoff. The token is already cached for its one-hour life.

Effort is small-to-medium and stays entirely behind the mock + `ALLOW_REAL_PHI` gates.

---

## 7. Recommended sequence (checklist)

1. Register an athena Preview app (API Access = non-Certified, Category = 2-Legged, Auth = Secret); capture `client_id` / `client_secret`; note `practiceid` `195900`.
2. Set the env vars in a staging deploy: `ATHENA_USE_MOCK=false`, `ALLOW_REAL_PHI=true` (staging only, every other rail mock).
3. Validate OAuth + the encounter pull + the canonical mapping against Preview (synthetic data).
4. In Admin, apply "Clinical only (EHR billing)"; confirm the sidebar reflects athena-mode.
5. Wire the EHR Pull sync and the payer-id crosswalk (section 6).
6. In parallel (business / legal): confirm/sign the athenahealth BAA + the cloud BAAs (Google Cloud, Neon, Anthropic, Stedi); book a HIPAA security review.
7. Provision production: real `practiceid`, prod base URL, deploy to Cloud Run + Neon, `ALLOW_REAL_PHI=true`, run `npm run readiness` until green.
8. Phase 2: wire athena payments → Found-Money to prove recovered revenue on athena's data.

---

## Appendix A — If you sell it later (multi-tenant)

You are building for your own practice. **If that ever changes** and you distribute CodeCompanion to
*other* athena practices, a different set of obligations switches on. None of this is needed now — but
here is what comes back, and the good news is the architecture is already built for it.

- **athena Marketplace path (athena API ToS §1(f)).** Distributing access to other practices needs
  athena's **prior written approval** via the Marketplace partner program: **API certification + a
  data-security review + a workflow assessment (~6–12 weeks)**, a **HITRUST self-assessment within 90
  days** of going live, and a **15–30% revenue share** on Marketplace-channeled revenue (no setup or
  interface fees).
- **Confidentiality vs. the behavioral corpus (athena ToS §7).** Data from the API is athena
  **Confidential Information**, usable "only as necessary in exercising your rights" and not
  disclosable to third parties without consent. Building the **cross-tenant de-identified corpus**
  (Rung 2 / the network-effect moat) from athena-sourced data needs counsel to clear it first — the
  PHI→corpus gate is clean in code, but this is a contractual layer on top.
- **Fraud & abuse (athena ToS §8).** A third-party billing arrangement with contingency / percentage
  pricing, plus AI coding suggestions, carries Anti-Kickback / Stark / False-Claims exposure → get
  healthcare-regulatory counsel on the pricing model. (For your own practice there is no outside
  biller taking a cut, so this does not apply today.)
- **Already built for multi-tenant.** Postgres RLS, `tenant_id` isolation, and the de-id corpus gate
  (`lib/corpus`) are in the repo, running as a single tenant today. When you scale, the foundation is
  there — and Cloud Run + Neon scale to many tenants without a re-platform.
